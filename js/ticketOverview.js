import {store} from './state.js';
import {ticketEventPortfolio32} from './ticketPortfolio32.js';
import {ownEvent1100} from './runtimeOwnership1100.js';
import {session} from './cloud.js';
import {loadProductAdvancedStyles} from './productAdvancedStyles.js';
import {openPersonalAction641} from './personalActionExecution641.js';
import {modal} from './utils.js';

const OWNER='product.ticketOverview';
const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const money=v=>new Intl.NumberFormat('cs-CZ',{style:'currency',currency:'CZK',maximumFractionDigits:0}).format(Number(v||0));
const upper=v=>String(v||'').toUpperCase();

function data(){
 const s=store.get(),p=ticketEventPortfolio32(s),items=s.ticketBook?.items||[],master=s.ticketBook?.masterMeta||null;
 const transfer=items.filter(x=>['SOLD_UNDELIVERED','TRANSFER_REQUIRED','SOLD_WAITING_TRANSFER'].includes(upper(x.market_status||x.workflow)));
 const payout=items.filter(x=>['PAYOUT_WAIT','SOLD_WAITING_PAYMENT','WAITING_PAYOUT'].includes(upper(x.market_status||x.workflow)));
 const issues=items.filter(x=>x.issue);
 const ticketTasks=(s.tasks||[]).filter(x=>!['DONE','CLOSED','ARCHIVED','RESOLVED','CANCELLED','CANCELED'].includes(upper(x.status))).filter(x=>upper(x.area)==='TICKETS'||upper(x.category)==='VIAGOGO').sort((a,b)=>Number(b.priority||0)-Number(a.priority||0));
 const attention=[];
 if(issues.length)attention.push({tone:'bad',title:issues.length+' reklamace / sporné prodeje',detail:'V Excelu jsou vedené jako červené, ale mají příznak reklamace. Zkontroluj je před ostatními listingy.',action:'advanced'});
 if(transfer.length)attention.push({tone:'bad',title:transfer.length+' prodejů čeká na převod',detail:'Prodáno, ale předání kupujícímu ještě není dokončené.',action:'sync'});
 if(p.missingListed)attention.push({tone:'warn',title:p.missingListed+' listingů nemá cenu',detail:'Bez skutečné list ceny OS neumí hlídat prodej správně.',action:'advanced'});
 if(p.staleMarket)attention.push({tone:'warn',title:p.staleMarket+' eventů má starý market',detail:'Aktualizuj tržní cenu před dalším rozhodnutím.',action:'advanced'});
 if(payout.length)attention.push({tone:'warn',title:payout.length+' prodejů čeká na payout',detail:'Zkontroluj stav platby z marketplace.',action:'sync'});
 for(const t of ticketTasks.slice(0,4))attention.push({tone:Number(t.priority||0)>=90?'bad':'warn',title:t.title||'Ticket úkol',detail:t.notes||'Otevřený ticketový úkol.',action:'task',taskId:t.id});
 return {s,p,items,master,issues,transfer,payout,ticketTasks,attention:attention.slice(0,6)};
}
function attentionHtml(rows){if(!rows.length)return '<div class="pr1300-empty">Žádný transfer, payout ani pricing problém teď nehoří.</div>';return '<div class="pr1300-attention">'+rows.map(x=>'<button type="button" data-ticket-action="'+x.action+'" '+(x.taskId?'data-ticket-task-id="'+esc(x.taskId)+'"':'')+'><i class="pr1300-dot '+x.tone+'"></i><span><b>'+esc(x.title)+'</b><small>'+esc(x.detail)+'</small></span><em>řešit →</em></button>').join('')+'</div>'}
function eventRows(events){if(!events.length)return '<div class="pr1300-empty">Žádné aktivní vstupenky.</div>';return events.map((x,i)=>'<button type="button" class="pr1300-row pr1300-clickrow" data-ticket-event="'+i+'"><div class="pr1300-row-main"><b>'+esc(x.name)+'</b><small>'+x.qty+' ks · kapitál '+money(x.capitalAtRisk)+' · '+esc(x.nextAction||'sledovat')+'</small></div><div class="pr1300-row-side '+(x.priority>=85?'bad':x.priority>=70?'warn':'')+'">'+x.priority+'/100 <span class="os1500-row-arrow">→</span></div></button>').join('')}
function primaryAction(x,topEvent){if(x?.action==='sync')return '<button class="pr1300-btn primary" type="button" data-ticket-sync>Synchronizovat Viagogo →</button>';if(x?.action==='advanced')return '<button class="pr1300-btn primary" type="button" data-ticket-advanced>Otevřít případ →</button>';if(x?.action==='task'&&x.taskId)return '<button class="pr1300-btn primary" type="button" data-ticket-task-id="'+esc(x.taskId)+'">Vyřešit úkol →</button>';if(topEvent)return '<button class="pr1300-btn primary" type="button" data-ticket-event="0">Otevřít event →</button>';return '<button class="pr1300-btn primary" type="button" data-ticket-task>＋ Úkol k ticketům</button>'}

export function renderTicketOverview(){
 const host=document.querySelector('#ticketIntelView');if(!host)return false;if(host.dataset.productAdvanced==='1')return true;const d=data(),top=d.p.top,hasAttention=d.attention.length>0,hasEvents=d.p.events.length>0;
 const attentionBlock=hasAttention?'<section class="pr1300-panel"><div class="pr1300-panel-head"><h2>Teď řešit</h2><button class="pr1300-btn" type="button" data-ticket-advanced>Ticket desk</button></div>'+attentionHtml(d.attention)+'</section>':'';
 const eventsBlock=hasEvents?'<section class="pr1300-panel"><div class="pr1300-panel-head"><h3>Aktivní eventy</h3><span>nejvyšší priorita nahoře</span></div>'+eventRows(d.p.events)+'</section>':'';
 const clearBlock=!hasAttention&&!hasEvents?'<section class="pr1300-panel pr1327-ticket-clear"><div class="pr1300-panel-head"><h2>Portfolio je klidné</h2><button class="pr1300-btn" type="button" data-ticket-advanced>Ticket desk</button></div><div class="pr1300-empty">Žádný transfer, payout, pricing problém ani aktivní event teď nevyžaduje zásah.</div></section>':'';
 host.innerHTML='<div class="pr1300-shell" data-ticket-overview>' +
 '<div class="pr1300-head"><div><div class="pr1300-kicker">Vstupenky</div><h1>Flipování 2024–2026.</h1><p>'+(d.master?'Zdroj: '+d.master.sourceSheets.join(' · ')+' · barva určuje stav.':'Nejdřív transfery, payouty a otevřené případy. Aktivní eventy až potom.')+'</p></div><span class="pr1300-status '+(d.issues.length||d.transfer.length?'bad':hasAttention?'warn':'good')+'">'+(hasAttention?d.attention.length+' k řešení':'klid')+'</span></div>' +
 '<section class="pr1320-now"><div><div class="pr1300-kicker">Teď</div><h2>'+esc(d.attention[0]?.title||top?.name||'Nic urgentního')+'</h2><p>'+esc(d.attention[0]?.detail||top?.nextAction||'Aktivní portfolio teď nevyžaduje okamžitý zásah.')+'</p></div><div class="pr1320-now-actions">'+primaryAction(d.attention[0],top)+'</div></section>' +
 '<div class="pr1320-meta"><span>Aktivní eventy: '+d.p.totalEvents+'</span><span>Aktivní kusy: '+d.p.queue.activeQty+'</span><span>Reklamace: '+d.issues.length+'</span><span>Kapitál: '+money(d.p.totalCapital)+'</span></div>' +
 (d.master?'<div class="pr1320-meta"><span>2024: '+Number(d.master.years?.[2024]?.qty||0)+' ks</span><span>2025: '+Number(d.master.years?.[2025]?.qty||0)+' ks</span><span>2026: '+Number(d.master.years?.[2026]?.qty||0)+' ks</span><span>Celkem: '+Number(d.master.ticketQty||0)+' ks</span></div><div class="pr1320-meta"><span>Nákup: '+money(d.master.buyTotalCzk)+'</span><span>Prodej: '+money(d.master.sellTotalCzk)+'</span><span>Hrubý rozdíl: '+money(d.master.grossMarginCzk)+'</span></div>':'') +
 attentionBlock + eventsBlock + clearBlock +
 '<div class="pr1300-actions"><button class="pr1300-btn" type="button" data-ticket-task>＋ Úkol k ticketům</button><button class="pr1300-btn" type="button" data-ticket-sync>Synchronizovat Viagogo</button><button class="pr1300-btn" type="button" data-ticket-advanced>Ticket desk</button></div></div>';
 host.__ticketOverview1500=d;
 if(!host.dataset.ticketOverviewBound){host.dataset.ticketOverviewBound='1';ownEvent1100(OWNER,host,'click',async e=>{if(e.target.closest('[data-ticket-task]')){window.dispatchEvent(new CustomEvent('kamil:capture',{detail:'ticket-task'}));return}const taskBtn=e.target.closest('[data-ticket-task-id]');if(taskBtn){const item=(store.get().tasks||[]).find(x=>String(x.id)===taskBtn.dataset.ticketTaskId);if(item)await openPersonalAction641({id:'task:'+item.id,kind:'task',title:item.title||'Ticket úkol',why:item.notes||'Aktuální ticketová záležitost.',next:'Vyřešit nebo posunout další krok.',route:'tickets'});renderTicketOverview();return}if(e.target.closest('[data-ticket-sync]')||e.target.closest('[data-ticket-action="sync"]')){if(!await session()){window.dispatchEvent(new CustomEvent('kamil:cloud-login'));return}const m=await import('./ticketGmailSync429.js');await m.syncTicketGmail429?.();renderTicketOverview();return}if(e.target.closest('[data-ticket-advanced]')||e.target.closest('[data-ticket-action="advanced"]')){host.dataset.productAdvanced='1';await loadProductAdvancedStyles(['./ticket68.css','./globalFintech137.css','./ticketWorkspace210.css','./ticketDesk353.css','./ticketDesk355.css','./ticketDesk356.css']);const m=await import('./ticketAdvanced100.js');await m.renderTicketPage100?.();return}const eventBtn=e.target.closest('[data-ticket-event]');if(eventBtn){const current=host.__ticketOverview1500||data(),x=current.p.events[Number(eventBtn.dataset.ticketEvent)];if(!x)return;const choice=await modal('Aktivní event',`<div class="card"><div class="eyebrow">VSTUPENKY</div><h2>${esc(x.name)}</h2><div class="row"><span>Aktivní kusy</span><b>${Number(x.qty||0)}</b></div><div class="row"><span>Kapitál v riziku</span><b>${money(x.capitalAtRisk)}</b></div><div class="row"><span>Priorita</span><b>${Number(x.priority||0)}/100</b></div><p class="muted">${esc(x.nextAction||'Sledovat event.')}</p></div>`,[{label:'Otevřít Ticket desk',value:'advanced',primary:true},{label:'Zavřít',value:null}]);if(choice==='advanced'){host.dataset.productAdvanced='1';await loadProductAdvancedStyles(['./ticket68.css','./globalFintech137.css','./ticketWorkspace210.css','./ticketDesk353.css','./ticketDesk355.css','./ticketDesk356.css']);const m=await import('./ticketAdvanced100.js');await m.renderTicketPage100?.()}return}})}

 window.__KAMIL_TICKET_OVERVIEW__={healthy:true,events:d.p.totalEvents,transfer:d.transfer.length,issues:d.issues.length,attention:d.attention.length,masterId:d.s.ticketBook?.masterId||null,historyQty:d.master?.ticketQty||null,dynamicPrimary:true,directEvents:true,at:Date.now()};return true;
}