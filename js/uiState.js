import {h} from './utils.js';
import {viewDefinition41} from './viewRegistry.js';

export function beginViewLoad(view){const def=viewDefinition41(view),host=document.querySelector(`#${def.host}`)||document.querySelector(`#${def.legacyHost||''}`);if(!host)return null;host.setAttribute('aria-busy','true');if(!host.children.length)host.innerHTML=`<div class="card" data-view-loading><div class="eyebrow">${h(def.title)}</div><h2>Načítám…</h2><p class="muted">Připravuju uložená data a potřebné moduly.</p></div>`;return host}
export function completeViewLoad(view){const def=viewDefinition41(view),host=document.querySelector(`#${def.host}`)||document.querySelector(`#${def.legacyHost||''}`);host?.removeAttribute('aria-busy');host?.querySelector('[data-view-loading]')?.remove();return host}
export function renderViewError(view,error,retry){const def=viewDefinition41(view),host=document.querySelector(`#${def.host}`)||document.querySelector(`#${def.legacyHost||''}`);if(!host)return false;host.removeAttribute('aria-busy');host.innerHTML=`<div class="card" data-view-error><div class="eyebrow">${h(def.title)}</div><h2>Sekci se nepodařilo načíst</h2><p class="muted">${h(error?.message||String(error||'Neznámá chyba'))}</p><div class="row-actions"><button class="btn primary" type="button" data-view-retry>Zkusit znovu</button><button class="btn" type="button" data-open-ops>Diagnostika</button></div><p class="muted">Uložená data nebyla smazána.</p></div>`;host.querySelector('[data-view-retry]')?.addEventListener('click',()=>retry?.());host.querySelector('[data-open-ops]')?.addEventListener('click',async()=>{try{const m=await import('./controlOperations1047.js');m.openOperations1047?.()}catch{}});return true}
