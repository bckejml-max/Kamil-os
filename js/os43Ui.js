import {store} from './state.js';import {os43,markAction43} from './os43.js';import {h,toast} from './utils.js';
const id='os43Hub',czk=v=>`${Math.round(Number(v)||0).toLocaleString('cs-CZ')} Kč`;function nav(t){window.dispatchEvent(new CustomEvent('kamil:navigate',{detail:t||'today'}))}
function render(){const root=document.querySelector('#todayView');if(!root)return;document.querySelector(`#${id}`)?.remove();const s=store.get(),o=os43(s),el=document.createElement('div');el.id=id;el.className='card brain34-card';el.dataset.os43='';const top=o.queue.top;el.innerHTML=`<div class="card-head"><div><div class="eyebrow">KAMIL OS / 43.0 EXECUTION LAYER</div><h2>${h(o.brief.headline)}</h2><p class="muted">Z intelligence dělá pořadí konkrétních kroků. Kritické akce zůstávají pouze návrhem.</p></div><span class="status ${o.score.score<60?'bad':o.score.score<80?'warn':'good'}">${o.score.label} ${o.score.score}/100</span></div><div class="metric-strip"><div class="metric"><span>Fronta</span><b>${o.queue.rows.length}</b></div><div class="metric"><span>Kritické</span><b>${o.queue.critical}</b></div><div class="metric"><span>SLA po termínu</span><b>${o.score.sla}</b></div><div class="metric"><span>Viditelný dopad</span><b>${czk(o.queue.totalImpactCzk)}</b></div></div>${top?`<div class="decision-note"><strong>Next Best Action</strong><div class="brain34-row"><div><b>${h(top.title)}</b><small>${h(top.urgency)} · priorita ${top.priority}${top.reason?` · ${h(top.reason)}`:''}</small></div><button class="btn primary" data-os43-nav="${h(top.target)}">Otevřít</button></div></div>`:''}<div class="decision-note"><strong>Dnešní execution queue</strong><div class="brain34-list">${o.queue.rows.slice(0,5).map(x=>`<div class="brain34-row" data-row="${h(x.id)}"><div><b>#${x.rank} ${h(x.title)}</b><small>${h(x.source)} · ${h(x.urgency)} · ${x.priority}/100${x.impactCzk?` · ${czk(x.impactCzk)}`:''}</small></div><div class="brain34-actions"><button class="btn" data-open="${h(x.target)}">Otevřít</button><button class="btn" data-done="${h(x.id)}">Hotovo</button><button class="btn" data-snooze="${h(x.id)}">Odložit 24 h</button></div></div>`).join('')||'<div class="empty">Fronta je čistá.</div>'}</div></div><div class="decision-note"><strong>Bezpečnost</strong><div class="muted">Žádné automatické obchody, přecenění, mazání ani odesílání. „Hotovo / odložit“ pouze zapisuje Decision Memory a upraví další prioritizaci.</div></div>`;root.querySelector('[data-os42]')?.insertAdjacentElement('beforebegin',el)||root.querySelector('.view-head')?.insertAdjacentElement('afterend',el)||root.prepend(el);el.querySelectorAll('[data-os43-nav],[data-open]').forEach(b=>b.onclick=()=>nav(b.dataset.os43Nav||b.dataset.open));const find=id=>o.queue.rows.find(x=>x.id===id);el.querySelectorAll('[data-done]').forEach(b=>b.onclick=()=>{const r=find(b.dataset.done);if(!r)return;markAction43(s,r,'DONE');store.persist();toast('Označeno jako hotovo');render()});el.querySelectorAll('[data-snooze]').forEach(b=>b.onclick=()=>{const r=find(b.dataset.snooze);if(!r)return;markAction43(s,r,'SNOOZED');store.persist();toast('Odloženo o 24 hodin');render()})}
let q=false;function schedule(){if(q)return;q=true;queueMicrotask(()=>{q=false;render()})}function start(){store.subscribe(()=>{if(document.querySelector('#view-today')?.classList.contains('on'))schedule()});window.addEventListener('kamil:navigate',schedule);setTimeout(render,760)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
