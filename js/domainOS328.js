import {store} from './state.js';
import {buildFinanceCommand258,openFinanceCommand258} from './financeCommand258.js';
import {buildTicketCommand268,openTicketCommand268} from './ticketCommand268.js';
import {buildIntelligence318} from './intelligence318.js';
const num=x=>Number.isFinite(Number(x))?Number(x):0,upper=x=>String(x||'').toUpperCase(),done=x=>['DONE','CLOSED','ARCHIVED','PAID'].includes(upper(x?.status));
export function buildMoneyOS319(s=store.get()){const f=buildFinanceCommand258(s);return{version:319,...f,liquidity:f.cash,invested:f.investments,netWorth:f.netWorth,freeCapital:f.deployable,monthlyBurn:f.monthlyOut}}
export function buildInvestmentDesk320(s=store.get()){const f=buildFinanceCommand258(s);const rows=f.positions.map(x=>{let action='HOLD',reason='Bez silného signálu ke změně.';if(x.weight>.35){action='REDUCE';reason='Příliš vysoká koncentrace portfolia.'}else if(x.roi<-.2){action='REVIEW';reason='Výrazná ztráta vyžaduje kontrolu teze.'}else if(x.roi>.35){action='TRIM/TAKE PROFIT';reason='Silný zisk, vhodné zkontrolovat velikost pozice.'}return{...x,action,reason,recheckAt:new Date(Date.now()+7*86400000).toISOString()}});return{version:320,rows,freeCapital:f.deployable}}
export function buildTicketDesk321(s=store.get()){const t=buildTicketCommand268(s);return{version:321,...t,signals:t.rows.map(x=>({...x,signal:x.safeBuy?'BUY':x.sellTiming==='REPRICE NOW'?'SELL/REPRICE':x.sellTiming==='REVIEW PRICE'?'REVIEW':'HOLD'}))}}
export function buildCapitalAllocator322(s=store.get()){const f=buildFinanceCommand258(s),t=buildTicketCommand268(s),candidates=[{key:'cash',label:'Držet rezervu',score:f.cash<f.reserveTarget?95:45,amount:Math.max(0,f.reserveTarget-f.cash),reason:f.cash<f.reserveTarget?'Rezerva je pod cílem.':'Rezerva je pokrytá.'},{key:'xtb',label:'Investice',score:f.deployable>0&&f.concentration.length===0?70:45,amount:f.deployable,reason:f.concentration.length?'Nejdřív zkontrolovat koncentraci portfolia.':'Je dostupný kapitál nad rezervu.'},{key:'tickets',label:'Vstupenky',score:t.opportunities.length?65:20,amount:t.opportunities.reduce((a,x)=>a+num(x.netSafeMaxBuyPrice||x.maxBuyPrice),0),reason:t.opportunities.length?`${t.opportunities.length} bezpečně odemčených příležitostí.`:'Žádný bezpečně odemčený BUY.'}].sort((a,b)=>b.score-a.score);return{version:322,candidates,best:candidates[0]}}
export function buildPropertyOS323(s=store.get()){const rows=[...(s.property?.items||[]),...(s.homeProjects||[]),...(s.documents?.items||[])].filter(x=>!done(x));return{version:323,rows,urgent:rows.filter(x=>/práv|vada|rekonstruk|dům|nemovit/i.test(`${x.title||''} ${x.category||''}`)).slice(0,10)}}
export function buildManagerOS324(s=store.get()){const rows=[...(s.tasks||[]),...(s.delegations||[])].filter(x=>!done(x)).filter(x=>/faktur|zakáz|dodavat|karta|pks|cpi|zbroj|práce|pracovn/i.test(`${x.title||''} ${x.area||''} ${x.category||''}`));return{version:324,rows,overdue:rows.filter(x=>{const d=Date.parse(x.due||x.followUpAt||0);return d&&d<Date.now()})}}
export function buildWeeklyReview327(s=store.get()){const intel=buildIntelligence318(s),money=buildMoneyOS319(s),tickets=buildTicketDesk321(s),manager=buildManagerOS324(s);return{version:327,urgent:intel.inbox.urgent.length,followups:intel.followups.rows.length,changes:intel.changes.newCount,netWorth:money.netWorth,freeCapital:money.freeCapital,ticketActive:tickets.active.length,ticketRealized:tickets.realized,workOpen:manager.rows.length,generatedAt:new Date().toISOString()}}
export function buildDomainOS328(s=store.get()){return{version:328,money:buildMoneyOS319(s),investments:buildInvestmentDesk320(s),tickets:buildTicketDesk321(s),capital:buildCapitalAllocator322(s),property:buildPropertyOS323(s),manager:buildManagerOS324(s),weekly:buildWeeklyReview327(s),architecture:{renderer:'kamilCore312',intelligence:'intelligence318',actionModel:'312',design:'307',qa:'308'}}}
function money=x=>`${Math.round(num(x)).toLocaleString('cs-CZ')} Kč`;
function openWeekly(){const m=buildWeeklyReview327();window.dispatchEvent(new CustomEvent('kamil:detail-drawer',{detail:{title:'Weekly Review',html:`<div class="os303-list"><div class="os303-row"><span><b>${m.urgent} urgentních věcí</b><small>${m.followups} follow-upů · ${m.changes} změn</small></span></div><div class="os303-row"><span><b>${money(m.netWorth)}</b><small>čisté jmění · ${money(m.freeCapital)} volný kapitál</small></span></div><div class="os303-row"><span><b>${m.ticketActive} aktivních vstupenek</b><small>realizováno ${money(m.ticketRealized)}</small></span></div><div class="os303-row"><span><b>${m.workOpen} pracovních věcí</b><small>otevřeno v Manager OS</small></span></div></div>`}}))}
function command(q){const n=String(q||'').toLowerCase();if(/pen[ií]ze|finance|xtb|invest/.test(n)){openFinanceCommand258();return true}if(/vstup|ticket/.test(n)){openTicketCommand268();return true}if(/t[yý]den|weekly|review/.test(n)){openWeekly();return true}if(/kapit[aá]l|kam d[aá]t pen[ií]ze/.test(n)){const m=buildCapitalAllocator322();window.dispatchEvent(new CustomEvent('kamil:detail-drawer',{detail:{title:'Capital Allocator',html:`<div class="os303-list">${m.candidates.map(x=>`<div class="os303-row"><span><b>${x.label}</b><small>${x.reason}</small></span><strong>${x.score}</strong></div>`).join('')}</div>`}}));return true}return false}
function wireCommand325(){const input=document.querySelector('#commandInput'),go=document.querySelector('#commandGo');if(!input||!go||go.dataset.os325==='1')return;go.dataset.os325='1';go.addEventListener('click',e=>{if(command(input.value)){e.stopImmediatePropagation();e.preventDefault()}},true);input.addEventListener('keydown',e=>{if(e.key==='Enter'&&command(input.value)){e.stopImmediatePropagation();e.preventDefault()}},true)}
function decorate(){wireCommand325();const more=document.querySelector('#moreView');if(more&&!more.querySelector('[data-domain328]')){const b=document.createElement('button');b.type='button';b.className='card';b.dataset.domain328='1';b.innerHTML='<div class="eyebrow">KAMIL OS 3.0</div><h3>Weekly Review</h3><p class="muted">Jedno místo pro finance, vstupenky, práci a osobní priority.</p>';b.addEventListener('click',openWeekly);more.prepend(b)}window.__KAMIL_DOMAIN328__={version:328,model:buildDomainOS328(),openWeekly,at:Date.now()}}
let timer=0;const schedule=()=>{clearTimeout(timer);timer=setTimeout(decorate,120)};
export function installDomainOS328(){store.subscribe?.(schedule);window.addEventListener('kamil:view-change',schedule);schedule();setTimeout(schedule,600);window.__KAMIL_COMMAND325__={run:command};window.__KAMIL_DIRECT_ACTIONS326__={supported:['done','tomorrow','detail','finance','tickets','weekly','capital']}}
