const VERSION=453,N=v=>Number(v||0);let bound=false,timer=0;
const fmt=v=>Number.isFinite(Number(v))?`${Math.round(Number(v)).toLocaleString('cs-CZ')} Kč`:'—';
const cat=n=>{const s=String(n||'').toLowerCase();if(/sparta|slavia|fotbal|davis|cup/.test(s))return'SPORT';if(/clash/.test(s))return'COMBAT';if(/katseye|asap|iheart|concert|koncert/.test(s))return'CONCERT';return'OTHER'};
const age=r=>{const t=Date.parse(r.imported_at||r.created_at||'');return Number.isFinite(t)?Math.max(0,(Date.now()-t)/864e5):0};
function build(){const pred=window.__KAMIL_TICKET_PREDICT433__,ops=window.__KAMIL_TICKET_RISKOPS446__;if(!pred?.models?.length)return null;const rows=pred.models.map(x=>{const cap=N(x.r.buy_total_czk)||N(x.r.buy_each_czk)*Math.max(1,N(x.r.qty)||1),days=Math.max(1,age(x.r)),ev=N(x.predict?.opt?.ev),bucket=days<=14?'0–14':days<=30?'15–30':days<=60?'31–60':'60+',eff=cap?ev/cap/days*1000:0;return{...x,capital453:{capital:cap,days,bucket,efficiency:eff,category:cat(x.r.event_name)}}}),buckets={},categories={};for(const x of rows){buckets[x.capital453.bucket]=(buckets[x.capital453.bucket]||0)+x.capital453.capital;categories[x.capital453.category]=(categories[x.capital453.category]||0)+x.capital453.capital}const total=rows.reduce((a,x)=>a+x.capital453.capital,0),cash=ops?.cash||{},p7=N(cash.receivable)*.45,p14=N(cash.receivable)*.75,p30=N(cash.receivable),sorted=[...rows].sort((a,b)=>b.capital453.efficiency-a.capital453.efficiency),sold=window.__KAMIL_TICKET_EXECUTION_OUTCOME450__?.rows?.filter(x=>x.sold)||[],recentLoss=sold.slice(0,5).some(x=>N(x.profit)<0),guard=recentLoss?'ACTIVE':'OK';return{rows,buckets,categories,total,cashForecast:{d7:p7,d14:p14,d30:p30},ranking:sorted,lossRecoveryGuard:guard}}
function ensureCss(){if(document.querySelector('style[data-cp453]'))return;const s=document.createElement('style');s.dataset.cp453='1';s.textContent=`#ticketIntelView .cp453{margin:0 0 12px;border:1px solid #2f4c5d;border-radius:11px;background:#091923;color:#dceaf3;padding:9px 11px}.cp453-grid{display:grid;grid-template-columns:1.35fr repeat(5,1fr);gap:8px}.cp453 span{display:block;font-size:8px;font-weight:900;color:#7898ad;text-transform:uppercase}.cp453 b{display:block;margin-top:3px;font-size:11px}.cp453 small{display:block;margin-top:2px;color:#7890a1;font-size:8.5px}.cp453-warn{color:#efc17c}@media(max-width:850px){.cp453-grid{grid-template-columns:1fr 1fr}}`;document.head.appendChild(s)}
function paint(m){ensureCss();const host=document.querySelector('#ticketIntelView [data-mr452]')||document.querySelector('#ticketIntelView .td331');if(!host)return;let box=document.querySelector('#ticketIntelView [data-cp453]');if(!box){box=document.createElement('section');box.dataset.cp453='1';box.className='cp453';host.insertAdjacentElement('afterend',box)}const slow=m.ranking[m.ranking.length-1];box.innerHTML=`<div class="cp453-grid"><div><span>CAPITAL PLANNER · OS453</span><b>${fmt(m.total)} aktivního kapitálu</b><small>Aging, turnover, cash deployment a category exposure.</small></div><div><span>CASH 7D</span><b>${fmt(m.cashForecast.d7)}</b></div><div><span>CASH 14D</span><b>${fmt(m.cashForecast.d14)}</b></div><div><span>CASH 30D</span><b>${fmt(m.cashForecast.d30)}</b></div><div><span>60+ DAYS</span><b>${fmt(m.buckets['60+']||0)}</b></div><div><span>LOSS RECOVERY</span><b class="${m.lossRecoveryGuard==='ACTIVE'?'cp453-warn':''}">${m.lossRecoveryGuard}</b><small>${slow?`nejnižší efektivita: ${slow.r.event_name}`:''}</small></div></div>`}
function refresh(){const m=build();if(!m)return;window.__KAMIL_TICKET_CAPITAL453__={version:VERSION,healthy:true,...m,at:Date.now()};document.documentElement.dataset.ticketCapital453='1';paint(m);window.dispatchEvent(new CustomEvent('kamil:ticket-capital453-updated',{detail:{total:m.total}}))}
function schedule(ms=700){clearTimeout(timer);timer=setTimeout(refresh,ms)}
export function installTicketCapitalPlanner453(){if(bound)return;bound=true;for(const ev of ['kamil:view-change','kamil:ticket-predict433-updated','kamil:ticket-riskops446-updated','kamil:ticket-outcome450-updated'])window.addEventListener(ev,()=>schedule());schedule(12500);setInterval(()=>schedule(0),60000)}
