import {buildFinanceCommand258,openFinanceCommand258} from './financeCommand258.js';
const money=x=>`${Math.round(Number(x||0)).toLocaleString('cs-CZ')} Kč`,esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function action(p){if(p.weight>.35)return{code:'REDUCE',tone:'critical',label:'SNÍŽIT / ZKONTROLOVAT',reason:'Pozice má příliš vysokou váhu portfolia.'};if(p.roi<-.2)return{code:'REVIEW',tone:'warning',label:'REVIEW',reason:'Výrazná ztráta vyžaduje kontrolu investiční teze.'};if(p.roi>.35)return{code:'TRIM',tone:'success',label:'ZVÁŽIT ZISK',reason:'Silný zisk — zkontroluj velikost pozice a další katalyzátory.'};return{code:'HOLD',tone:'neutral',label:'DRŽET / REVIEW',reason:'Bez silného signálu ke změně.'}}
function render(){const host=document.querySelector('#moneyView');if(!host)return false;const m=buildFinanceCommand258(),rows=m.positions.map(p=>({...p,decision:action(p)}));host.innerHTML=`<div class="money340"><section class="money340-hero"><small>KAMIL OS 4.0 · MONEY DESK</small><h1>Peníze a investice bez šumu.</h1><p>Likvidita, čisté jmění, koncentrace a investiční review na jednom místě.</p><button data-money340-detail>Otevřít Finance Command</button></section><section class="money340-kpis"><div><span>Čisté jmění</span><b>${money(m.netWorth)}</b></div><div><span>Hotovost</span><b>${money(m.cash)}</b></div><div><span>Investice</span><b>${money(m.investments)}</b></div><div><span>Volný kapitál</span><b>${money(m.deployable)}</b></div></section><section class="money340-panel"><header><div><small>INVESTMENT ACTION CENTER</small><h2>Co zkontrolovat</h2></div></header><div class="money340-list">${rows.length?rows.map(p=>`<article><div><span class="money340-pill ${p.decision.tone}">${p.decision.label}</span><h3>${esc(p.ticker||p.name||'Pozice')}</h3><p>${esc(p.decision.reason)}</p></div><div class="money340-right"><b>${money(p.value)}</b><small>${(p.weight*100).toFixed(1)} % portfolia · P/L ${money(p.pl)}</small></div></article>`).join(''):'<div class="money340-empty">Žádné XTB pozice v uložených datech.</div>'}</div></section><section class="money340-panel"><header><div><small>CASHFLOW</small><h2>Výhled hotovosti</h2></div></header><div class="money340-forecast">${m.forecast.map(x=>`<div><span>${x.days} dní</span><b>${money(x.balance)}</b></div>`).join('')}</div></section></div>`;host.querySelector('[data-money340-detail]')?.addEventListener('click',openFinanceCommand258);window.__KAMIL_MONEY_DESK340__={version:340,model:m,at:Date.now()};return true}
function visible(){const h=document.querySelector('#moneyView');return !!h&&!h.classList.contains('hidden')&&getComputedStyle(h).display!=='none'}
export function installMoneyDesk340(){let t=0;const schedule=()=>{clearTimeout(t);t=setTimeout(()=>{if(visible())render()},80)};window.addEventListener('kamil:view',e=>{if(e.detail?.view==='money')schedule()});window.addEventListener('kamil:view-change',schedule);document.addEventListener('click',e=>{if(e.target?.closest?.('[data-view="money"]'))schedule()},true);window.__KAMIL_MONEY_DESK340__={version:340,render};setTimeout(schedule,350)}
