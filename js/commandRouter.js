import {h,modal} from './utils.js';
import {searchGlobalIndex,openSearchHit} from './searchIndex.js';

const norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
const slashKey=q=>norm(q).split(/\s+/)[0].replace(/^\//,'');
const ONE_KEYS=new Set(['os','one','inbox','cash','money','tickets','bets','betting','property','byt','family','home','docs','timeline','opportunities','find','health','gaps','legacy']);
const OPS_KEYS=new Set(['ops','operations','watches','snapshots']),CONTROL_KEYS=new Set(['system','health2','backup','automations']),CLEANUP_KEYS=new Set(['cleanup','retire','legacy-status']);
let installed=false;
async function openOne(raw){const key=slashKey(raw),m=await import('./oneOS967.js'),map={os:'autopilot',one:'autopilot',inbox:'inbox',cash:'money',money:'money',tickets:'tickets',bets:'betting',betting:'betting',property:'property',byt:'property',family:'family',home:'home',docs:'docs',timeline:'timeline',opportunities:'opportunities',find:'search',health:'health',gaps:'gaps',legacy:'control'};return m.openFeature?.(map[key]||'autopilot')}
async function openOps(){const m=await import('./controlOperations1047.js');return m.openOperations1047?.()}
async function openControl(){const m=await import('./controlPlane1037.js');return m.openControlPlane1037?.()}
async function openCleanup(){const m=await import('./legacyCleanup987.js');return m.openLegacyCleanup987?.()}
async function runCopilot(raw){const [{buildCopilot842},{answer840,contextualFollowups795}]=await Promise.all([import('./copilotFeedback842.js'),import('./copilot840.js')]);const model=await buildCopilot842(),answer=answer840(raw,model);if(!answer)return false;const confidence=Number(model?.confidence?.overall??model?.confidence??0),health=Number(model?.health||0),follow=contextualFollowups795?.(raw)||[],body=`<div class="card"><div class="eyebrow">KAMIL OS · COPILOT</div><h2>${h(answer)}</h2><div class="row"><span>Confidence</span><b>${Number.isFinite(confidence)?confidence.toFixed(0):'—'} %</b></div><div class="row"><span>OS Health</span><b>${Number.isFinite(health)?health.toFixed(0):'—'} %</b></div><p class="muted">Read-only odpověď z aktuálních dat. Finanční, ticket ani betting akce se automaticky neprovádějí.</p></div>${follow.length?`<div class="card"><div class="eyebrow">NAVAZUJÍCÍ DOTAZY</div>${follow.slice(0,4).map(x=>`<div class="row"><span>${h(x)}</span></div>`).join('')}</div>`:''}`;const choice=await modal('Kamil OS Copilot',body,[{label:'Otevřít detail Copilota',value:'open'},{label:'Zavřít',value:null,primary:true}]);if(choice==='open'){const m=await import('./copilotFeedback842.js');return m.openCopilot842?.()}return true}
async function special(raw,{execute=true}={}){const q=String(raw||'').trim();if(!q)return false;const key=slashKey(q),n=norm(q);if(q.startsWith('/')&&ONE_KEYS.has(key)){if(execute)await openOne(q);return true}if(q.startsWith('/')&&OPS_KEYS.has(key)){if(execute)await openOps();return true}if(q.startsWith('/')&&CONTROL_KEYS.has(key)){if(execute)await openControl();return true}if(q.startsWith('/')&&CLEANUP_KEYS.has(key)){if(execute)await openCleanup();return true}try{const s=await import('./strategyCommand790.js');if(s.isStrategyQuestion790?.(q)){if(execute)await s.executeStrategyCommand790(q);return true}}catch{}try{const c=await import('./commandCopilot840.js');if(c.canHandleCopilot841?.(q)){if(execute)await runCopilot(q);return true}}catch{}try{const c=await import('./capitalCommand100.js');if(c.isCapitalQuestion100?.(q)){if(execute)await c.openCapitalDecision100(q);return true}}catch{}if(/^(stav|status)\s+(os|systemu)$/.test(n)){if(execute)await openOps();return true}return false}

function renderIndexResults(q,hits=searchGlobalIndex(q)){
 const box=document.querySelector('#commandResults');if(!box)return false;if(!hits.length)return false;box.classList.remove('hidden');box.innerHTML=hits.map((x,i)=>`<button class="search-row" type="button" role="option" data-global-search="${i}"><div><b>${h(x.title)}</b><div class="muted">${h(x.kind)}${x.detail?` · ${h(x.detail)}`:''}</div></div></button>`).join('');box.querySelectorAll('[data-global-search]').forEach((b,i)=>b.addEventListener('click',()=>{openSearchHit(hits[i]);box.classList.add('hidden')}));document.querySelector('#commandInput')?.setAttribute('aria-expanded','true');return true
}
export async function executeCentralCommand(raw=''){
 const q=String(raw||'').trim();if(!q)return false;if(await special(q,{execute:true}))return true;
 const hits=searchGlobalIndex(q);if(hits.length===1){openSearchHit(hits[0]);return true}if(hits.length>1){renderIndexResults(q,hits);return true}
 const x=await import('./command.js'),parsed=x.parse?.(q);if(parsed&&parsed.type!=='free'&&parsed.type!=='empty'){x.execute(q);return true}
 const choice=await modal('Příkazu nerozumím',`<div class="card"><b>${h(q)}</b><p class="muted">V Kamil OS jsem nenašel odpovídající položku ani známou akci. Nic se nezapíše bez dalšího potvrzení.</p></div>`,[{label:'Vytvořit z toho úkol',value:'task',primary:true},{label:'Zrušit',value:null}]);if(choice==='task'){const m=await import('./personalCapture643.js');return m.openPersonalCapture643?.('task',{title:q,area:'Osobní',category:'Osobní'})}return true
}
function routePreview(label,detail,q){const box=document.querySelector('#commandResults');if(!box)return false;box.classList.remove('hidden');box.innerHTML=`<button class="search-row" type="button" role="option" data-central-command><div><b>${h(label)}</b><div class="muted">${h(detail)}</div></div></button>`;box.querySelector('[data-central-command]')?.addEventListener('click',()=>{box.classList.add('hidden');void executeCentralCommand(q)});document.querySelector('#commandInput')?.setAttribute('aria-expanded','true');return true}
export async function renderCentralCommandResults(raw=''){
 const q=String(raw||'').trim(),box=document.querySelector('#commandResults');if(!box)return false;box.setAttribute('role','listbox');if(!q){box.classList.add('hidden');box.innerHTML='';document.querySelector('#commandInput')?.setAttribute('aria-expanded','false');return true}const key=slashKey(q);
 if(q.startsWith('/')&&ONE_KEYS.has(key))return routePreview(`One OS · /${key}`,'Otevřít canonical One OS funkci',q);if(q.startsWith('/')&&OPS_KEYS.has(key))return routePreview('Operations','Health, snapshoty, watches a automations',q);if(q.startsWith('/')&&CONTROL_KEYS.has(key))return routePreview('Control Plane','Diagnostika, data a bezpečné operace',q);if(q.startsWith('/')&&CLEANUP_KEYS.has(key))return routePreview('Legacy cleanup','Stav konsolidace bez automatického mazání',q);
 try{const s=await import('./strategyCommand790.js');if(s.isStrategyQuestion790?.(q))return routePreview('Strategie z aktuálních dat','Read-only vyhodnocení',q)}catch{}try{const c=await import('./commandCopilot840.js');if(c.canHandleCopilot841?.(q))return routePreview('Kamil OS Copilot','Read-only odpověď z canonical dat',q)}catch{}try{const c=await import('./capitalCommand100.js');if(c.isCapitalQuestion100?.(q))return routePreview('Capital Allocation','Porovnat možnosti bez automatického provedení',q)}catch{}
 if(renderIndexResults(q))return true;try{const x=await import('./commandSearch610.js');x.installCommandSearch610?.();if(x.renderExtendedResults610?.(q)){document.querySelector('#commandInput')?.setAttribute('aria-expanded','true');return true}}catch{}const m=await import('./command.js');m.renderResults(q);document.querySelector('#commandInput')?.setAttribute('aria-expanded',box.classList.contains('hidden')?'false':'true');return true
}
function consume(input){const raw=String(input?.value||'').trim();if(!raw)return false;input.value='';input.setAttribute('aria-expanded','false');document.querySelector('#commandResults')?.classList.add('hidden');void executeCentralCommand(raw).catch(error=>console.error('[command-router]',error));return true}
export function installCommandRouter(){if(installed||window.__KAMIL_COMMAND_ROUTER__?.installed)return true;installed=true;const input=document.querySelector('#commandInput');if(input){input.setAttribute('role','combobox');input.setAttribute('aria-autocomplete','list');input.setAttribute('aria-controls','commandResults');input.setAttribute('aria-expanded','false')}document.addEventListener('keydown',event=>{const el=event.target?.closest?.('#commandInput');if(!el||event.key!=='Enter'||!String(el.value||'').trim())return;event.preventDefault();event.stopImmediatePropagation();consume(el)},true);document.addEventListener('click',event=>{if(!event.target?.closest?.('#commandGo'))return;const el=document.querySelector('#commandInput');if(!String(el?.value||'').trim())return;event.preventDefault();event.stopImmediatePropagation();consume(el)},true);window.__KAMIL_COMMAND_ROUTER__={installed:true,owner:'central',execute:executeCentralCommand,render:renderCentralCommandResults,search:searchGlobalIndex,at:Date.now()};return true}
