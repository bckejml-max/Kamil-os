const mem=new Map();globalThis.localStorage={getItem:k=>mem.get(k)??null,setItem:(k,v)=>mem.set(k,String(v)),removeItem:k=>mem.delete(k)};
const now=new Date(),fresh=now.toISOString();
localStorage.setItem('kamil-os-market-quotes-32',JSON.stringify({fetchedAt:fresh,quotes:[{symbol:'EURCZK=X',price:25,currency:'CZK',asOf:fresh,sourceUrl:'https://finance.yahoo.com/quote/EURCZK=X'},{symbol:'VWCE.DE',price:170,currency:'EUR',asOf:fresh,sourceUrl:'https://finance.yahoo.com/quote/VWCE.DE'}]}));
const {xtbPortfolioAudit}=await import('./js/xtbAudit24.js');
const {xtbContributionPlan32,xtbContribution32Contract}=await import('./js/xtbContribution32.js');
const {ticketEventPortfolio32,ticketActionQueue32,ticketPortfolio32Contract}=await import('./js/ticketPortfolio32.js');
const assert=(x,m)=>{if(!x)throw new Error(m)};
const state={meta:{schemaVersion:80},financePlan:{plannedInvestment:25000},xtbHub:{asOf:fresh,accounts:{czk:{currency:'CZK',value:100000,positions:[{ticker:'EUNA.DE',name:'Core Global Aggregate Bond',category:'ETF',value:50000,volume:400,net_profit_pct:0},{ticker:'EXUS.DE',name:'MSCI World ex USA',category:'ETF',value:50000,volume:50,net_profit_pct:0}]},eur:{currency:'EUR',value:1000,positions:[{ticker:'VWCE.DE',name:'FTSE All-World',category:'ETF',value:1000,volume:5.8,net_profit_pct:0}]}}},xtbReport:{asOf:fresh},xtbStrategy:{overrides:{}},ticketBook:{items:[{id:'c1',name:'Clash 17 - A2-1',date:'2026-10-24',workflow:'LISTED',qty:2,buy:2482,listPrice:0,marketPrice:0,transferStatus:'UNKNOWN'},{id:'c2',name:'Clash 17 - A3-2',date:'2026-10-24',workflow:'HOLD',qty:2,buy:2482,listPrice:0,marketPrice:0,transferStatus:'READY'},{id:'a1',name:'ASAP - 415',date:'2026-10-04',workflow:'LISTED',qty:2,buy:5312,listPrice:0,marketPrice:0,transferStatus:'READY'}],watchlist:[],history:[]}};
const audit=xtbPortfolioAudit(state);assert(audit.valuationComplete,'FX-backed audit should be complete');assert(Math.round(audit.total)===125000,`EUR account must convert to CZK, got ${audit.total}`);const eur=audit.currencyBreakdown.find(x=>x.currency==='EUR');assert(Math.round(eur.czkValue)===25000&&eur.rateToCzk===25,'EUR breakdown must use 25 CZK/EUR');
const contribution=xtbContributionPlan32(state);assert(contribution.ok&&!contribution.blocked,'fresh complete contribution plan expected');assert(contribution.plannedCzk===25000&&contribution.allocations.reduce((s,x)=>s+x.amountCzk,0)===25000,'full 25k must be allocated');assert(contribution.allocations.some(x=>x.ticker==='VWCE.DE'),'all-world core candidate should be usable');assert(!xtbContribution32Contract.autoTrade&&xtbContribution32Contract.requiresCompleteFx,'contribution safety contract');
localStorage.setItem('kamil-os-market-quotes-32',JSON.stringify({fetchedAt:fresh,quotes:[]}));const missing=xtbPortfolioAudit(state);assert(!missing.valuationComplete&&missing.missingFx.includes('EUR'),'missing EUR FX must be explicit');assert(xtbContributionPlan32(state).blocked,'contribution must block without FX');
const events=ticketEventPortfolio32(state);assert(events.totalEvents===2,'Clash bundles must consolidate into one event');const clash=events.events.find(x=>x.name==='Clash 17');assert(clash&&clash.qty===4&&clash.capitalAtRisk===4964,'Clash event exposure aggregation');assert(clash.rows.length===2,'Clash event contains two bundles');const queue=ticketActionQueue32(state);assert(queue.needsInput>=2&&queue.rows[0].score>=queue.rows.at(-1).score,'ticket action queue prioritized');assert(events.missingListed>=2,'listed tickets without actual price surfaced');assert(!ticketPortfolio32Contract.autoPrice&&!ticketPortfolio32Contract.autoSell&&!ticketPortfolio32Contract.staleMarketCanDrivePrice,'ticket safety contract');
console.log('KAMIL OS 32.5 PROFIT CONTROL UNIT PASS');
