import {buildPersonalToday} from './js/personalToday26.js';
import {appendDecisionJournal,decisionJournalReview} from './js/decisionJournal31.js';
const assert=(x,m)=>{if(!x)throw new Error(m)};
const now=new Date('2026-08-20T10:00:00+02:00'),base={financePlan:{currency:'CZK',cashNow:200000,reserveFloor:50000,cashflow:[]},tasks:[],personalAdmin:{items:[]},familyHome:{members:[]},assetBook:{items:[]},emergencyFile:{contacts:[],assets:[]},calendar:{events:[]},debtBook:{items:[]},audit:[],xtbStrategy:{closedTickers:{},overrides:{}},ticketBook:{items:[],watchlist:[]}};
const beforeState=structuredClone(base);beforeState.xtbHub={asOf:'2026-08-20T08:00:00Z',accounts:{czk:{currency:'CZK',value:100000,positions:[{ticker:'WDAY',name:'Workday',category:'STOCK',value:10000,net_profit_pct:-10}]}}};beforeState.ticketBook.items=[{id:'ticket-1',name:'Test koncert',workflow:'LISTED',date:'2026-08-26',qty:1,buy:1000,listPrice:1800,floorPrice:1200}];
const currentState=structuredClone(base);currentState.xtbHub={asOf:'2026-08-20T09:00:00Z',accounts:{czk:{currency:'CZK',value:100000,positions:[{ticker:'WDAY',name:'Workday',category:'STOCK',value:10000,net_profit_pct:30}]}}};currentState.ticketBook.items=[{id:'ticket-1',name:'Test koncert',workflow:'LISTED',date:'2026-08-22',qty:1,buy:1000,listPrice:1800,floorPrice:1200}];
const before=buildPersonalToday(beforeState,now),current=buildPersonalToday(currentState,now),oldX=before.find(x=>x.id==='WDAY'),newX=current.find(x=>x.id==='WDAY'),oldT=before.find(x=>x.id==='ticket-1'),newT=current.find(x=>x.id==='ticket-1');
assert(oldX?.observed?.pnlPct===-10&&Number.isFinite(oldX.observed.weightPct),'XTB safe observations missing');assert(oldT?.observed?.workflow==='LISTED'&&Number.isFinite(oldT.observed.days),'ticket observations missing');
const saved=appendDecisionJournal([],before,now);assert(saved.added>=2,'journal should record current top decisions');const state={decisionJournal:{items:saved.items}},review=decisionJournalReview(state,current,new Date('2026-08-20T12:00:00+02:00'));
const x=review.items.find(i=>i.key==='money|WDAY'),t=review.items.find(i=>i.key==='tickets|ticket-1');assert(x?.actionChanged&&x.currentAction==='TRIM','XTB journal action change missing');assert(x.observedShift?.kind==='PNL_SHIFT'&&x.observedShift.delta===40,'XTB observed P/L shift missing');assert(t?.actionChanged&&t.currentAction==='SELL','ticket journal action change missing');assert(!JSON.stringify(saved.items).match(/password|seed|cvv/i),'journal must not introduce secret fields');
console.log('DECISION JOURNAL 31.1 INTEGRATION PASS');
